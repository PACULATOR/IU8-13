#include <iostream>
#include <windows.h>
using namespace std;
int main() {
    double x;
    cout << "Введите необходимый объём топлива без учта экономии (л)" << endl;
    cin >> x;
    cout << " необходимый объём топлива с уячётом экоомии =" << x*0.5*0.8*0.7 <<"ыыыы";
    return 0;
}
